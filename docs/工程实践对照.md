# 工程实践对照（手册体检表）

> 依据《新产品开发手册：MVP → 完整版的工程实践清单》对 FocusStation(v2.0.10) 的逐条体检。
> 整理日期：2026-09-08。判定：✅ 已满足 ｜ ⚠️ 部分/缺口 ｜ ❌ 缺失 ｜ N/A 不适用。

## 阶段 0 · 动手写代码之前

| # | 事项 | 判定 | 一句话现状 |
|---|---|---|---|
| 0.1 | 核心事件可观测 | ✅ | 计时/操作全在本机，不需要外部信号源 |
| 0.2 | 假设清单 / 风险登记 | ✅(本次补) | 已新增 `docs/假设清单.md`，从代码防御提炼为显式清单 |
| 0.3 | 外部依赖失效行为 | ✅ | AI 失败保留原内容不崩；WebDAV 用 ETag 防覆盖、404/412 重试 |

## 阶段 1 · MVP（第一天就要做）

| # | 事项 | 判定 | 现状 |
|---|---|---|---|
| 1.1 | 凭据卫生 | ✅ | Windows 加密存密钥，绝不明文；测试断言"磁盘无明文、备份不含凭据" |
| 1.2 | 状态 schema 带版本号 | ✅ | `Version=1` + `AiDefaults.Migrate` 迁移，不做大爆炸重构 |
| 1.3 | at-least-once + 幂等 | ✅ | RunId 去重、原子写、合并幂等，均有测试覆盖 |
| 1.4 | fail-open 边界 | ✅ | AI/同步挂了都不影响主计时功能 |
| 1.5 | 测试隔离和打标 | ✅ | `UI_TEST` 隔离目录 + 本地假服务，测试不碰真凭据 |
| 1.6 | 结构化日志 | ✅ | `RuntimeEvent` 本地记录，设置页可查看、筛选、导出 CSV 和清空 |
| 1.7 | 第二次复制就抽模块 | ✅ | UI/TaskSupport/Planner/Store 已抽成共享模块 |
| 1.8 | 部署运维脚本化 | ✅ 部分 | 有 build/test 脚本，缺发布/安装自动化 |

## 阶段 2 · 跑通后的演进期

| # | 事项 | 判定 | 现状 |
|---|---|---|---|
| 2.1 | 镜像不变量 | ⚠️ 部分 | 状态字段简单，但未显式写成"布尔 ≡ 数值条件" |
| 2.2 | 单调性闸门 | ✅ | 合并按 `UpdatedUtc` 取最新；挑战计时不超界（有测试） |
| 2.3 | 假成功检查 | ✅ | 不只看 HTTP 2xx，还校验返回体结构 |
| 2.4 | 状态可审计 | ✅ 部分 | 有 `.bak`/`.damaged` 备份；数据本身非逐条历史 |
| 2.5 | 调度双保险 / heartbeat | N/A | 本机应用，无外部调度，不需要 |
| 2.6 | 并发防护 | ✅ 基础 | 单实例 Mutex + 原子写文件 |
| 2.7 | 平台/渠道注册表 | ✅ 适度 | 接口地址可配，没过度抽象（做对了） |
| 2.8 | 通道矩阵 | ✅ 足够 | 单 AI + 单 WebDAV，各自独立 try/catch |

## 阶段 3 · 完整版 / 长期维护

| # | 事项 | 判定 | 现状 |
|---|---|---|---|
| 3.1 | 故障闭环 | ✅ | `docs/事故与回归映射.md` 按功能分区、事故编号关联回归断言 |
| 3.2 | 文档与代码互锁 | ✅(本次补) | 已建 `README.md` 索引 + 本文档；代码点注释待深化 |
| 3.3 | 修订记录 | ✅(本次补) | 已新增 `docs/CHANGELOG.md` |
| 3.4 | 运维入口 | ✅ | 测试 API / 立即同步 / 导入导出 按钮齐备 |

## 反模式核查

- 密钥入库 ❌否 ｜ 三遍不抽模块 ❌否 ｜ 测试硬编码时间锚点 ❌否（都用相对时间）｜ 不写失效行为 ⚠️多在代码里未成文（已由假设清单补齐）。

## 结论

代码在 MVP 与可靠性上已成熟；短板集中在"写下来的治理资产"，本次已补齐文档层（假设清单、事件日志待 dev、修订记录、对照表、README）。手册里 N/A 部分（调度/heartbeat/多平台）对本机应用不适用，不补。
